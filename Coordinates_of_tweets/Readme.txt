the tweets.py file is a file which takes in the coordintaes from the json file created from running the tweet search python file and then it extracts the coordinates and stores them in a dataframe which are then tracked on a map using basemap library in python.

